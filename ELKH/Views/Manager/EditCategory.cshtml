@model ELKH.Models.CategoryModel

@{
    ViewData["Title"] = "Edit Category";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<main role="main" aria-labelledby="main-heading">
    <div class="container mt-4 pb-5" style="max-width: 500px">

        <header class="mb-4">
            <h1 id="main-heading" class="fw-bold text-primary mb-0">Edit Category</h1>
            <p class="text-muted small">Rename an existing category</p>
        </header>

        <div class="kawaii-card">
            <form asp-action="EditCategory" method="post">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="PkCategoryId" />

                <div class="mb-3">
                    <label asp-for="CategoryName" class="form-label fw-semibold"></label>
                    <input asp-for="CategoryName" class="form-control input-kawaii" autofocus />
                    <span asp-validation-for="CategoryName" class="text-danger small"></span>
                </div>

                <div class="d-flex gap-2 justify-content-end">
                    <a asp-action="Categories" class="btn btn-outline-secondary rounded-pill px-4">Cancel</a>
                    <button type="submit" class="btn btn-primary rounded-pill px-4">
                        <i class="bi bi-floppy me-1" aria-hidden="true"></i>Save
                    </button>
                </div>
            </form>
        </div>
    </div>
</main>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
