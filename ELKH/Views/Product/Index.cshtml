@model IEnumerable<ELKH.ViewModels.ProductVM>

@*
╔==================================================================================╗
║ Product Index View - Optimized with Reusable Partial Components                ║
║ Uses partial views for better maintainability and code reuse                    ║
╚==================================================================================╝
*@

@{
    ViewData["Title"] = "Products - Browse Our Sticker Collection";

    // ===============================================================================
    // ║ ViewBag Data Extraction and Processing                                     ║
    // ║ Extract pagination, search, and filtering parameters from controller       ║
    // ===============================================================================

    var products   = Model.ToList();
    var total      = ViewBag.Total      as int? ?? products.Count;
    var hasMore    = ViewBag.HasMore    as bool? ?? false;
    var search     = ViewBag.Search     as string;
    var categoryId = ViewBag.CategoryId as int?;
    var currentSort = ViewBag.Sort as string ?? "name_asc";

    // Prepare ViewModels for partial views
    var searchFilterVM = new ELKH.ViewModels.ProductSearchFilterVM
    {
        Search = search,
        CategoryId = categoryId,
        CurrentSort = currentSort,
        Categories = ViewBag.Categories
    };

    var sortingVM = new ELKH.ViewModels.ProductSortingVM
    {
        Search = search,
        CategoryId = categoryId,
        CurrentSort = currentSort
    };

    }

<!-- ================================================================================ -->
<!-- ║ Page Header & Search Results Display with Enhanced Accessibility           ║ -->
<!-- ================================================================================ -->

<section class="kawaii-hero" aria-labelledby="products-heading">
    <!-- Skip Links for Keyboard Navigation -->
    <nav class="skip-links sr-only-focusable" aria-label="Skip navigation">
        <a href="#product-search-input" class="skip-link btn-kawaii btn-secondary-kawaii">Skip to search</a>
        <a href="#productGrid" class="skip-link btn-kawaii btn-secondary-kawaii">Skip to products</a>
    </nav>

    <div class="container-kawaii">
        <div class="text-center">
            <h1 id="products-heading" class="kawaii-cloud-title display-title sparkle">
                <i class="bi bi-grid me-3" aria-hidden="true" style="color: var(--butter);"></i>
                Products
            </h1>
            <p class="kawaii-subtitle">
                Browse our amazing collection of stickers!
            </p>

            @if (!string.IsNullOrWhiteSpace(search) || categoryId.HasValue)
            {
                <div class="mt-4">
                    <div class="kawaii-card surface-sky d-inline-block px-4 py-2">
                        <div class="badge-group" role="group" aria-label="Active filters">
                            @if (!string.IsNullOrWhiteSpace(search))
                            {
                                <span class="kawaii-badge badge-primary" aria-label="Search filter: @search">
                                    <i class="bi bi-search me-1" aria-hidden="true"></i>@search
                                </span>
                            }
                            @if (categoryId.HasValue && ViewBag.CategoryName != null)
                            {
                                <span class="kawaii-badge badge-secondary ms-1" aria-label="Category filter: @ViewBag.CategoryName">
                                    <i class="bi bi-tag-fill me-1" aria-hidden="true"></i>@ViewBag.CategoryName
                                </span>
                            }
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>

@* ================================================================================== *@
@* ║ Search Results Information Banner                                              ║ *@
@* ================================================================================== *@
@if (!string.IsNullOrWhiteSpace(search))
{
    var searchAlertVM = new ELKH.ViewModels.AlertBannerVM
    {
        Message = $"Search results for: <strong>&ldquo;{search}&rdquo;</strong>",
        AlertType = "alert-info",
        Icon = "info-fill",
        IconType = "bootstrap",
        IconAriaLabel = "Search results information"
    };

    <div role="status" aria-live="polite" aria-label="Search results notification">
        @await Html.PartialAsync("_AlertBannerPartial", searchAlertVM)
    </div>
}

<!-- ================================================================================ -->
<!-- ║ CSS Styles - Product Card Accessibility & Hover Animations                 ║ -->
<!-- ================================================================================ -->
<style>
    /* Product card hover effects for enhanced user interaction feedback */
    .product-card {
        cursor: pointer;
        transition: transform .15s ease, box-shadow .15s ease;
        position: relative;
    }

    .product-card:hover {
        transform: translateY(-3px);
        box-shadow: 0 .5rem 1rem rgba(0,0,0,.12) !important;
    }

    /* Enhanced focus management for product cards */
    .product-card:focus-within {
        outline: 2px solid #0066cc;
        outline-offset: 2px;
        transform: translateY(-2px);
        box-shadow: 0 .5rem 1rem rgba(0,0,0,.15) !important;
    }

    /* Ensure stretched link accessibility */
    .stretched-link:focus {
        z-index: 2;
    }

    /* Improve keyboard navigation for action buttons */
    .product-card .btn:focus {
        z-index: 3;
        position: relative;
    }

    /* Reduce motion for users who prefer it */
    @@media (prefers-reduced-motion: reduce) {
        .product-card {
            transition: none;
        }
        .product-card:hover {
            transform: none;
        }
    }

    /* Enhanced Skip Links */
    .skip-links {
        position: relative;
        z-index: 1050;
    }

    .skip-link {
        position: absolute !important;
        left: -10000px !important;
        top: auto !important;
        width: 1px !important;
        height: 1px !important;
        overflow: hidden !important;
    }

    .skip-link:focus {
        position: static !important;
        width: auto !important;
        height: auto !important;
        overflow: visible !important;
        margin: 0 0.5rem 0.5rem 0 !important;
        padding: 0.5rem 1rem !important;
        z-index: 1051 !important;
    }

    /* Enhanced Badge Groups */
    .badge-group .badge {
        margin-right: 0.25rem;
    }

    .badge-group .badge:last-child {
        margin-right: 0;
    }

    /* Screen Reader Only but Focusable Elements */
    .sr-only-focusable:not(:focus):not(:focus-within) {
        position: absolute !important;
        width: 1px !important;
        height: 1px !important;
        padding: 0 !important;
        margin: -1px !important;
        overflow: hidden !important;
        clip: rect(0, 0, 0, 0) !important;
        white-space: nowrap !important;
        border: 0 !important;
    }
</style>

@* ── Search and Filter Form ──────────────────────────────────────────── *@
@await Html.PartialAsync("_ProductSearchFilterPartial", searchFilterVM)

@* ── Sort Controls with kawaii styling ─────────────────────────────────────────────────── *@
<section class="container-kawaii mb-4">
    <div class="kawaii-card">
        <div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between">
            <div class="mb-2 mb-md-0">
                <h2 class="font-heading">
                    <i class="bi bi-collection me-2" style="color: var(--purple);" aria-hidden="true"></i>
                    Showing <span id="header-shown-count">@products.Count</span> of @total products
                </h2>
            </div>
            <div>
                @await Html.PartialAsync("_ProductSortingPartial", sortingVM)
            </div>
        </div>
    </div>
</section>

@* ── Enhanced Result count with kawaii styling ─────────────────────────────────── *@
@if (products.Any())
{
    <section class="container-kawaii mb-4">
        <div class="kawaii-panel surface-mint">
            <div class="results-summary" role="region" aria-labelledby="results-summary-heading">
                <h2 id="results-summary-heading" class="sr-only">Search Results Summary</h2>
                <div role="status" 
                     aria-live="polite" 
                     aria-label="Search results count"
                     id="results-announcement">
                    <p class="hierarchy-secondary mb-0" id="results-count">
                        <i class="bi bi-check-circle me-2" style="color: var(--purple);" aria-hidden="true"></i>
                        <strong id="shown-count">@products.Count</strong> of <strong>@total</strong> product@(total != 1 ? "s" : "") found
                        @if (!string.IsNullOrWhiteSpace(search)) { 
                            <span> matching <em>&ldquo;@search&rdquo;</em></span> 
                        }
                        @if (categoryId.HasValue && ViewBag.CategoryName != null)
                        {
                            <span> in category <em>@ViewBag.CategoryName</em></span>
                        }
                    </p>
                </div>
            </div>
        </div>
    </section>
}

@* ── Product grid ─────────────────────────────────────────────────────── *@
@if (!products.Any())
{
    var productEmptyStateVM = new ELKH.ViewModels.EmptyStateCardVM
    {
        Title = "No products found",
        Icon = "search",
        IconType = "bootstrap",
        Description = !string.IsNullOrWhiteSpace(search) 
            ? $"No products match your search for \"{search}\". Try adjusting your search terms or browse all products."
            : (categoryId.HasValue 
                ? "No products found in the selected category. Try browsing other categories or search for specific items."
                : "No products available at the moment."),
        ActionText = !string.IsNullOrWhiteSpace(search) || categoryId.HasValue ? "Browse All Products" : null,
        ActionController = !string.IsNullOrWhiteSpace(search) || categoryId.HasValue ? "Product" : null,
        ActionAction = !string.IsNullOrWhiteSpace(search) || categoryId.HasValue ? "Index" : null,
        ActionButtonClass = "btn-kawaii btn-primary-kawaii"
    };

    @await Html.PartialAsync("_EmptyStateCardPartial", productEmptyStateVM)
}
else
{
    <main role="main" aria-labelledby="products-main-heading" class="container-kawaii">
        <section class="product-results" aria-labelledby="products-heading">
            <h2 id="products-main-heading" class="sr-only">Product Catalog</h2>
            <h3 id="products-heading" class="sr-only">
                @total Product@(total != 1 ? "s" : "") 
                @if (!string.IsNullOrWhiteSpace(search)) { <text>matching "@search"</text> }
                @if (categoryId.HasValue && ViewBag.CategoryName != null) { <text>in @ViewBag.CategoryName</text> }
            </h3>

            <!-- Product Grid using kawaii styling -->
            <div id="productGrid" class="row g-4" role="grid" aria-label="Product grid with @products.Count items displayed">
                @foreach (var p in products)
                {
                    <div class="col-lg-3 col-md-4 col-sm-6" role="gridcell">
                        @await Html.PartialAsync("_ProductCard", p)
                    </div>
                }
            </div>

            @* ── Show More Button ─────────────────────────── *@
            @if (hasMore)
            {
                <section class="container-kawaii mt-5 text-center" id="show-more-section">
                    <button type="button"
                            id="showMoreBtn"
                            class="btn-kawaii btn-primary-kawaii radius-pill px-5 py-3 glow-on-hover"
                            data-offset="@products.Count"
                            data-total="@total"
                            data-search="@(search ?? "")"
                            data-category-id="@(categoryId?.ToString() ?? "")"
                            data-sort="@currentSort"
                            aria-label="Show more products">
                        <i class="bi bi-plus-circle me-2" aria-hidden="true"></i>
                        Show More
                        <span class="ms-2 small opacity-75">(@products.Count of @total)</span>
                    </button>
                </section>
            }
        </section>
    </main>
}

@section Scripts {
<script>
(function () {
    const btn = document.getElementById('showMoreBtn');
    if (!btn) return;

    const grid = document.getElementById('productGrid');
    let offset = parseInt(btn.dataset.offset, 10);
    const total = parseInt(btn.dataset.total, 10);
    const search = btn.dataset.search;
    const categoryId = btn.dataset.categoryId;
    const sort = btn.dataset.sort;

    btn.addEventListener('click', async function () {
        btn.disabled = true;
        const originalHtml = btn.innerHTML;
        btn.innerHTML = '<span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>Loading...';

        try {
            const params = new URLSearchParams();
            if (search) params.set('search', search);
            if (categoryId) params.set('categoryId', categoryId);
            params.set('sort', sort);
            params.set('offset', offset);

            const response = await fetch('/Product/LoadMore?' + params);
            if (!response.ok) throw new Error('Failed to load products');

            const html = await response.text();
            if (!html.trim()) {
                btn.closest('#show-more-section').remove();
                return;
            }

            grid.insertAdjacentHTML('beforeend', html);

            const shown = grid.querySelectorAll('[role="gridcell"]').length;
            offset = shown;

            const shownCountEl = document.getElementById('shown-count');
            const headerCountEl = document.getElementById('header-shown-count');
            if (shownCountEl) shownCountEl.textContent = shown;
            if (headerCountEl) headerCountEl.textContent = shown;
            grid.setAttribute('aria-label', 'Product grid with ' + shown + ' items displayed');

            if (shown >= total) {
                btn.closest('#show-more-section').remove();
            } else {
                btn.disabled = false;
                btn.innerHTML = '<i class="bi bi-plus-circle me-2" aria-hidden="true"></i>Show More <span class="ms-2 small opacity-75">(' + shown + ' of ' + total + ')</span>';
            }
        } catch (e) {
            btn.disabled = false;
            btn.innerHTML = '<i class="bi bi-exclamation-circle me-2" aria-hidden="true"></i>Error loading. Try again.';
            setTimeout(function() { btn.innerHTML = originalHtml; }, 3000);
        }
    });
})();
</script>
}
